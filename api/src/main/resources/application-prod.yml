spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: ells-landscaping

app:
  cors:
    allowed-origins: ${CORS_ORIGINS}
  mail:
    enabled: true

logging:
  level:
    com.studio.api: INFO
