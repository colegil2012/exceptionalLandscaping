# Copy to .env. Only PUBLIC_ vars reach the browser.

# Leave empty in dev — Vite proxies /api to localhost:8080.
# In production set to the deployed API origin, e.g. https://api.example.com
PUBLIC_API_BASE=
